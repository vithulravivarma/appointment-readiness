next thing for precheck
- need to make sure that like triggering the next one after the current appointment ends works correctly

recent summaries are bs just restating precheck stuff 
and when its a manual delegation the recent summaries still suck - they're not human readable and just regurtitate messags